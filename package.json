{
  "name": "fomrals",
  "version": "1.0.0",
  "description": "FomraLS Vercel serverless functions: market-intelligence data (api/*.js, backed by backend/src/routes|lib) and push/employee-auth admin endpoints. The app's core CRM data goes straight from Flutter to Supabase and does not use this backend.",
  "scripts": {
    "vercel-build": "node scripts/vercel-build.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  }
}
