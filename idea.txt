Review the existing implementation before making any changes. Do NOT recreate completed functionality. Identify what is already implemented and complete only the missing or broken parts. Preserve the existing APIs, authentication, permissions, routing, database schema, business logic, and enterprise UI.

1. Home Dashboard
- Reduce the Site Ageing card height by ~35%.
- Increase the width of the Pipeline Dashboard using the freed space.
- Reduce the Deal Terms Distribution donut chart size.
- Move the donut chart to the left.
- Place the legend vertically on the right side of the chart.
- Below the donut chart add two compact clickable summary cards:
  • Negotiation
  • Legal Progress
- Clicking either card should open the list of leads in that stage.

2. Profile Photo
- If no profile photo exists, show "Upload Profile Photo".
- If a profile photo already exists, change the action to "View / Edit Profile Photo".
- While selecting an image, open a circular photo cropper before upload.
- Save the cropped image using the existing storage/backend.
- Ensure the profile picture persists after refresh, logout/login, and app restart.
- Show the profile picture consistently everywhere avatars are displayed.

3. Add Lead
- After successfully fetching GPS coordinates, automatically display the captured location as a pinned marker on the embedded map.
- Ensure the displayed marker always matches the captured GPS coordinates.

4. Quick Actions
- Remove the "Upload Document" floating quick action.
- Do not affect the remaining quick actions.

5. Add Employee
- Fix the employee invitation flow.
- Employees are created successfully, but invitation emails fail with the current error.
- Investigate the existing invite implementation and repair it without changing authentication or user creation logic.

6. Application Icon
Replace the default Flutter application icon with the provided Fomra logo everywhere:
- Android launcher icon
- iOS app icon
- Windows executable/taskbar icon
- Web favicon
- macOS/Linux icon (if supported)
Use the existing launcher icon configuration (flutter_launcher_icons if already configured). Ensure the icon updates after rebuilding.

7. Sidebar Branding
- Remove the large white Fomra logo container from the top of the sidebar.
- Replace it with a clean, compact logo/header similar to modern enterprise applications.
- Show only a small centered Fomra logo/icon.
- Reduce unnecessary top spacing.
- Keep the blue sidebar background unchanged.
- Maintain correct appearance in both collapsed and expanded sidebar modes.

8. General
- Reuse existing components wherever possible.
- Do not duplicate widgets, routes, or logic.
- Preserve responsiveness and the current enterprise design language.
- Implement only the missing or broken functionality.